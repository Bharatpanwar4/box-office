const ActorsGrid = ({actors})=>
{
    return(
        <div>
           {/* { actors.map(data=>{<div key={data.people.id}>{data.people.name}</div>})} */}
        
        </div>)
}
export default ActorsGrid;